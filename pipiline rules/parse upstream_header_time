rule "parse upstream_header_time"
when
  has_field("upstream_header_time")
then
  let num = to_double($message.upstream_header_time) * to_double(1000);
  set_field("upstream_header_time_num", num);
end
